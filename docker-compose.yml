version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./frontend:/app/frontend
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./verktorg.db
      - JWT_SECRET=change-this-in-production
